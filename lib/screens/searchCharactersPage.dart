import 'package:flutter/material.dart';
class SearchCharactersPage extends StatelessWidget {
  const SearchCharactersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Text"),
      ),
      body: Container(
        color: Colors.grey,
      ),
    );
  }
}
